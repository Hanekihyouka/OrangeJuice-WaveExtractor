buildscript {
    repositories {
        gradlePluginPortal()

    }
    dependencies {
        classpath 'com.github.johnrengelman:shadow:8.1.1'
    }
}

plugins {
    id 'java'
    id 'com.github.johnrengelman.shadow' version '8.1.1'
}

apply plugin: 'java'
apply plugin: 'com.github.johnrengelman.shadow'

group = 'haneki'
version = '1.0'
sourceCompatibility = 1.8

repositories {

}

dependencies {

}


shadowJar {
    manifest {
        attributes(
                'Main-Class': 'haneki.waveextractor.WavePirate'
        )
    }

    from("./"){
        include 'build.gradle'
    }
}
